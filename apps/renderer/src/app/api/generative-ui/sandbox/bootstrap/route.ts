import { NextResponse } from "next/server";

const BOOTSTRAP = `(()=>{"use strict";const p=new URLSearchParams(location.hash.slice(1)),a=p.get("artifact"),c=p.get("channel");if(!a||!/^gui_[a-f0-9]{64}$/.test(a)||!c||!/^[A-Za-z0-9_-]{1,128}$/.test(c))return;const s=document.createElement("script");s.src="/api/generative-ui/artifacts/"+a;s.addEventListener("error",()=>parent.postMessage({bridgeVersion:1,channel:c,type:"telemetry",event:"render_error",code:"ARTIFACT_LOAD_FAILED"},"*"));document.head.appendChild(s);addEventListener("submit",e=>e.preventDefault(),true);addEventListener("click",e=>{const t=e.target;if(t instanceof HTMLAnchorElement){e.preventDefault();parent.postMessage({bridgeVersion:1,channel:c,type:"telemetry",event:"policy_violation",code:"NAVIGATION_BLOCKED"},"*")}},true)})();`;
export function GET(): NextResponse { return new NextResponse(BOOTSTRAP, { headers: { "content-type": "text/javascript; charset=utf-8", "cache-control": "public, max-age=31536000, immutable", "x-content-type-options": "nosniff" } }); }
